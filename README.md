# 📈 Crypto Price Tracker (Deribit API)

RESTful API сервис для мониторинга цен криптовалют (**BTC, ETH**) с биржи **Deribit**.
Система автоматически собирает данные **каждую минуту**, сохраняет их в **PostgreSQL** и предоставляет API для получения истории цен и их фильтрации.

---

## 🚀 Возможности

* **Автоматизированный сбор данных**
  Фоновые задачи **Celery** опрашивают API Deribit без блокировки основного приложения.

* **Гибкая конфигурация**
  Список отслеживаемых тикеров и параметры подключения задаются через переменные окружения.

* **Отказоустойчивость**
  Обработка сетевых ошибок, таймаутов и автоматическое восстановление соединений.

* **Docker Native**
  Полная изоляция сервисов (**API, Worker, Beat, Redis, Postgres**) для стабильного и воспроизводимого развертывания.

---

## 🛠 Технологии

* **FastAPI** — высокопроизводительный REST API
* **Celery & Redis** — очереди задач и планирование
* **PostgreSQL & SQLAlchemy** — надежное хранение данных и ORM
* **Aiohttp** — асинхронные HTTP-запросы к Deribit
* **Pydantic v2** — валидация данных и конфигурации

---

## 📁 Структура проекта

```plaintext
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models.py
│   ├── repository.py
│   ├── deribit_client.py
│   └── tasks.py
├── tests/
│   ├── __init__.py
│   ├── test_api.py
│   └── test_repository.py                # Unit и интеграционные тесты
├── docker-compose.yml       # Описание инфраструктуры
└── Dockerfile               # Инструкции сборки образа
```

---

## 🔧 Инструкция по развертыванию

### 1️⃣ Подготовка окружения

Клонируйте репозиторий и создайте файл `.env` в корне проекта:

```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=deribit_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

REDIS_HOST=redis
REDIS_PORT=6379

# Важно: тикеры передаются в формате JSON-массива
TICKERS=["btc_usd", "eth_usd"]
```

---

### 2️⃣ Запуск через Docker Compose

Соберите образы и запустите все сервисы:

```bash
docker-compose up -d --build
```

#### Что произойдет при запуске:

* Поднимется база данных **PostgreSQL**
* Запустится **Redis** как брокер сообщений
* Сервис **web** инициализирует таблицы
* **celery_beat** начнет планировать задачи раз в минуту
* **celery_worker** начнет сохранять цены в БД

---

### 3️⃣ Проверка работы

* **Swagger UI (API документация)**
  👉 [http://localhost:8000/docs](http://localhost:8000/docs)

* **Health Check**
  👉 [http://localhost:8000/](http://localhost:8000/)

---

## 🧪 Тестирование

Для запуска тестов внутри контейнера:

```bash
docker-compose exec web pytest
```

---

## 🎯 Design Decisions (Архитектурные решения)

### 1️⃣ Repository Pattern

Вся работа с базой данных вынесена в `PriceRepository`.

**Зачем:**
Изоляция SQL-логики от бизнес-логики API и задач Celery.
При смене БД или структуры таблиц изменения потребуются только в одном месте.

---

### 2️⃣ Отказ от `async with` в API-клиенте

Асинхронный контекстный менеджер был заменен на явное управление сессиями.

**Зачем:**
Это устраняет ошибки жизненного цикла (`Session is closed`) при работе Celery и упрощает повторное использование клиента в фоновых задачах.

---

### 3️⃣ Использование `pydantic-settings`

Конфигурация считывается напрямую из `.env` с автоматической проверкой типов.

**Зачем:**
Приложение не запустится с некорректными настройками (например, неверный формат тикеров или отсутствующий порт БД).

---

### 4️⃣ Решение проблем SSL и DNS в Docker

В `Dockerfile` добавлена установка `ca-certificates`.

**Зачем:**
Решает проблему `CERTIFICATE_VERIFY_FAILED` при HTTPS-запросах к Deribit из Docker-контейнеров (особенно на Windows / WSL2).

---

### 5️⃣ Индексация базы данных

Добавлен составной индекс `(ticker, timestamp)`.

**Зачем:**
Основные API-запросы фильтруют данные по тикеру и диапазону дат — индекс обеспечивает высокую производительность даже при миллионах записей.

---

## 👨‍💻 Автор

**Жалгасов Адильбек**
Проект выполнен в рамках **тестового задания**

---

